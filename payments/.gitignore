# Never commit keys or the local correlation/settlement store.
.env
.env.*
!.env.example
.paj-store/
# Durable crypto-funding deposit ledger + watchlist. This is money — never commit.
.funding-store/
*.log
